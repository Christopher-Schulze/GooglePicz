[package]
name = "sync"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
auth = { path = "../auth" }
api_client = { path = "../api_client" }
cache = { path = "../cache" }

[dev-dependencies]
tempfile = "3"
